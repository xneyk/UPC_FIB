//X41120 Vector of mountains
#include <iostream>
#include <vector>
using namespace std;

void ini_vector(vector<int>& v, int n) {
   for (int i = 0; i < n; ++i) cin >> v[i];
}

/**
 * PRE: |v| >= 3
 * POST: returns a vector containing the height of all the peaks in v (in the same order)
*/
vector<int> obtain_peaks(const vector<int>& v) {
   vector w ()
}

int main() {
   int n, x;
   cin >> n;
   vector<int> v(n);
   ini_vector(v, n);

}